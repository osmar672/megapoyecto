import type {SearchProvider} from '../../core/types';import {cafeteriaRepository} from './service';export const cafeteriaSearch:SearchProvider={id:'cafeteria-search',search:q=>cafeteriaRepository.list(q).map(p=>({id:p.id,category:'PRODUCTS',title:p.name,description:p.description,path:'/cafeteria',keywords:[p.category],allowedRoles:['ADMIN','TEACHER','STUDENT_FAMILY','STAFF'],source:'cafeteria'}))};
