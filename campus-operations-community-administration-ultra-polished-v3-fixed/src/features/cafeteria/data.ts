import type {CafeteriaProduct} from '../../core/types';export const cafeteriaSeed:CafeteriaProduct[]=Array.from({length:15},(_,i)=>({id:`prod${i+1}`,name:['Arroz con pollo','Jugo natural','Sandwich','Yogur','Ensalada'][i%5]+` ${i+1}`,description:'Producto disponible en la cafetería escolar.',price:700+(i%5)*250,category:(['COMIDAS','BEBIDAS','SNACKS','POSTRES','SALUDABLE'] as const)[i%5],availability:i%7===0?'LIMITED':'AVAILABLE'}));
