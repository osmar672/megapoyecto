import { makeWidget, FeatureWidget } from "../shared"; import { list } from "./service"; export const dashboardWidget=makeWidget("resources-widget","Equipamiento",86,()=>{const available=list().filter((x:any)=>String(x[x.length-1]).toLowerCase().includes("dispon")||String(x[x.length-1]).toLowerCase().includes("pend")||String(x[x.length-1]).toLowerCase().includes("abiert")||String(x[x.length-1]).toLowerCase().includes("aprob"));return <FeatureWidget title="Equipamiento" value={String(available.length)} detail="recursos disponibles"/>});
