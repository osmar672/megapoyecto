import {useMemo,useState} from "react"; import {Page,SearchFilter,Empty} from "../shared"; import {list,save,seed} from "./service";
export function CalendarPage(){const [items,setItems]=useState<any[]>(()=>{seed();return list()});const[q,setQ]=useState("");const[form,setForm]=useState({title:"",date:"",time:"",place:""});const filtered=useMemo(()=>items.filter(x=>x.join(" ").toLowerCase().includes(q.toLowerCase())),[items,q]);function add(){if(!form.title||!form.date||!form.time||!form.place)return;const next=[...items,[crypto.randomUUID(),form.title,"Evento institucional",form.date,form.time,form.place,"Agenda"]];setItems(next);save(next);setForm({title:"",date:"",time:"",place:""})}return <Page eyebrow="PLANIFICACIÓN" title="Calendario institucional" description="Agenda académica y operativa del campus."><div className="card form-card"><h2>Programar evento</h2><div className="form-grid"><input className="input" aria-label="Título" placeholder="Título" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><input className="input" type="date" aria-label="Fecha" value={form.date} onChange={e=>setForm({...form,date:e.target.value})}/><input className="input" type="time" aria-label="Hora" value={form.time} onChange={e=>setForm({...form,time:e.target.value})}/><input className="input" aria-label="Lugar" placeholder="Lugar" value={form.place} onChange={e=>setForm({...form,place:e.target.value})}/></div><button className="btn btn-primary" onClick={add}>Agregar al calendario</button></div><SearchFilter value={q} onChange={setQ} placeholder="Buscar eventos"/><div className="feature-grid">{filtered.map(x=><article className="card feature-card" key={x[0]}><span className="badge badge-info">{x[6]}</span><h3>{x[1]}</h3><p>{x[3]} · {x[4]} · {x[5]}</p></article>)}</div>{!filtered.length&&<Empty message="No hay eventos que coincidan con la búsqueda."/>}</Page>}
