import{useMemo,useState}from"react";import{Page,SearchFilter,Empty}from"../shared";import{list,save,seed}from"./service";export function TransportPassPage(){const[items,setItems]=useState<any[]>(()=>{seed();return list()});const[q,setQ]=useState("");const[route,setRoute]=useState("Ruta Centro");function add(){const next=[[crypto.randomUUID(),`Solicitud ${route}`,route,"Pendiente"],...items];setItems(next);save(next)}const filtered=useMemo(()=>items.filter(x=>x.join(" ").toLowerCase().includes(q.toLowerCase())),[items,q]);return<Page eyebrow="ACCESOS" title="Pases de transporte" description="Gestiona solicitudes de pase y asignaciones de transporte escolar."><div className="card form-card"><h2>Solicitar pase</h2><div className="form-grid"><select className="input" value={route} onChange={e=>setRoute(e.target.value)}><option>Ruta Norte</option><option>Ruta Centro</option><option>Ruta Sur</option><option>Ruta Especial</option></select></div><button className="btn btn-primary" onClick={add}>Enviar solicitud</button></div><SearchFilter value={q} onChange={setQ} placeholder="Buscar pases"/><div className="feature-grid">{filtered.map(x=><article className="card feature-card" key={x[0]}><span className="badge badge-info">{x[2]}</span><h3>{x[1]}</h3><p>Estado de solicitud</p><strong>{x[3]}</strong></article>)}</div>{!filtered.length&&<Empty message="No hay pases coincidentes."/>}</Page>}
