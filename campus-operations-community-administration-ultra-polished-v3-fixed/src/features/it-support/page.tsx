import{useMemo,useState}from"react";import{Page,SearchFilter,Empty}from"../shared";import{list,save,seed}from"./service";export function ItSupportPage(){const[items,setItems]=useState<any[]>(()=>{seed();return list()});const[q,setQ]=useState("");const[title,setTitle]=useState("");const filtered=useMemo(()=>items.filter(x=>x.join(" ").toLowerCase().includes(q.toLowerCase())),[items,q]);function add(){if(!title.trim())return;const next=[[crypto.randomUUID(),title,"Soporte general","Abierto"],...items];setItems(next);save(next);setTitle("")}return<Page eyebrow="MESA DE AYUDA" title="Soporte tecnológico" description="Gestiona solicitudes de soporte tecnológico y consulta su estado."><div className="card form-card"><h2>Abrir caso</h2><div className="form-grid"><input className="input" value={title} onChange={e=>setTitle(e.target.value)} placeholder="Describe el problema" aria-label="Problema"/></div><button className="btn btn-primary" onClick={add}>Abrir caso</button></div><SearchFilter value={q} onChange={setQ} placeholder="Buscar casos"/><div className="feature-grid">{filtered.map(x=><article className="card feature-card" key={x[0]}><span className="badge badge-info">{x[2]}</span><h3>{x[1]}</h3><p>Estado del caso</p><strong>{x[3]}</strong></article>)}</div>{!filtered.length&&<Empty message="No hay casos coincidentes."/>}</Page>}
