import type {SearchProvider} from '../../core/types';import {list} from './service';export const searchProvider:SearchProvider={id:'notifications-search',search:q=>list('usr_admin_001').filter(n=>`${n.title} ${n.message}`.toLowerCase().includes(q.toLowerCase())).map(n=>({id:n.id,category:'NOTIFICACIÓN',title:n.title,description:n.message,path:n.path??'/notifications',keywords:[n.type],allowedRoles:['ADMIN','TEACHER','STUDENT_FAMILY','STAFF'],source:'notifications-search'}))};
