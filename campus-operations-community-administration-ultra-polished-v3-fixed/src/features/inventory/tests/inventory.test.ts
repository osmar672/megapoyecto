import { describe,it,expect } from "vitest"; import { demoData } from "../data"; import { validateRequired } from "../validators"; describe("Inventario escolar",()=>{it("incluye datos demostrativos",()=>expect(demoData.length).toBeGreaterThanOrEqual(4));it("valida campos requeridos",()=>expect(validateRequired("dato válido")).toBe(true));});
