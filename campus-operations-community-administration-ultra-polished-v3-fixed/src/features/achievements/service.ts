import {notify} from '../notifications/service';
export interface Achievement{id:string;name:string;description:string;category:string;progress:number;unlocked:boolean;icon:string;unlockedAt?:string;}
const KEY='campus-operations.achievements.v1';
export const demoData:Achievement[]=[['ach-1','Primera semana','Completar siete días de actividad.','Participación',100,true,'1'],['ach-2','Asistencia perfecta','Mantener asistencia completa durante el periodo.','Rendimiento',82,false,'2'],['ach-3','Participación deportiva','Participar en una actividad deportiva.','Deportes',65,false,'3'],['ach-4','Participación cultural','Participar en una actividad cultural.','Cultura',100,true,'4'],['ach-5','Buen rendimiento','Mantener un rendimiento académico destacado.','Académico',72,false,'5'],['ach-6','Participación en actividades','Completar actividades institucionales.','Participación',88,false,'6'],['ach-7','Ayuda a la comunidad','Colaborar en una iniciativa comunitaria.','Comunidad',40,false,'7'],['ach-8','Actividades escolares completadas','Completar el conjunto de actividades asignadas.','Académico',58,false,'8']].map(([id,name,description,category,progress,unlocked,icon])=>({id,name,description,category,progress:Number(progress),unlocked:Boolean(unlocked),icon:String(icon)}));
export function list(){try{const raw=localStorage.getItem(KEY);return raw?JSON.parse(raw) as Achievement[]:demoData;}catch{return demoData;}}
export function seed(){if(!localStorage.getItem(KEY))localStorage.setItem(KEY,JSON.stringify(demoData));}
export function save(items:Achievement[]){localStorage.setItem(KEY,JSON.stringify(items));}
export function unlock(id:string,userId:string){const items=list();const target=items.find(a=>a.id===id);if(!target||target.unlocked)return false;target.unlocked=true;target.progress=100;target.unlockedAt=new Date().toISOString();save(items);window.dispatchEvent(new CustomEvent('achievement:unlocked',{detail:{id,userId}}));notify({userId,type:'ACHIEVEMENT',title:'Logro desbloqueado',message:`Has desbloqueado “${target.name}”.`,path:'/achievements',relatedId:id});return true;}
