import type { SearchProvider } from "../../core/search/searchProviderRegistry"; import { localStorageService } from "../../core/storage/storageService"; import { storageKeys } from "../../core/storage/storageKeys"; import type { Incident } from "../../core/types/domain";
const provider: SearchProvider = { id: "incidents", search(query, context) { const normalized = query.toLocaleLowerCase("es"); return localStorageService.get<Incident[]>(storageKeys.incidents, []).value.filter((incident) => (context.role === "ADMIN" || incident.reporterUserId === context.userId) && `${incident.type} ${incident.description} ${incident.location}`.toLocaleLowerCase("es").includes(normalized)).map((incident) => ({ id: incident.id, category: "EVENTS", title: incident.type, description: incident.description, path: "/incidents", keywords: [incident.location, incident.status], allowedRoles: [context.role], source: "incidents" })); } };
export default provider;
